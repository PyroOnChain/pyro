# build output, at any depth (foundry's out/, next's static export web/out/)
out/
cache/
broadcast/

# node / next
node_modules/
.next/
*.tsbuildinfo

# secrets and local config
.env
.env.*
!.env.example
.vercel/

# internal only: deploy runbook and accepted-risk notes, not for the public repo
LAUNCH.md

# tooling and OS
.claude/
.DS_Store
*.log

# whichever app lives at web/ (currently BRAWLZ, the path Pages builds)
/web/node_modules/
/web/.next/
/web/out/
/web/.env.local

# ladder capital, parked here after brawlz took over web/
ladder/node_modules/
ladder/.next/
ladder/out/
