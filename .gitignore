# build output, at any depth (foundry's out/, next's static export web/out/)
out/
cache/
broadcast/

# node / next
node_modules/
.next/
*.tsbuildinfo

# secrets and local config
.env
.env.*
!.env.example
.vercel/

# tooling and OS
.claude/
.DS_Store
*.log
