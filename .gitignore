# foundry (anchored to repo root so it does not swallow web/lib or keeper/lib)
/out/
/cache/
/lib/
/broadcast/

# node / next
node_modules/
.next/
*.tsbuildinfo

# secrets and local config
.env
.env.*
!.env.example
.vercel/

# tooling and OS
.claude/
.DS_Store
*.log
