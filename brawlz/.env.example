# Overrides only. The deployed mainnet factory is the default in lib/addresses.ts,
# so a plain build needs neither of these. Set them for a local anvil stack.
NEXT_PUBLIC_BATTLE_FACTORY=
NEXT_PUBLIC_RPC_URL=
